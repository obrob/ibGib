<UserControl x:Class="LearnLanguages.Silverlight.Views.ViewTranslationsItemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

  <Border Style="{StaticResource DebugBorder}" >
    <Border Style="{StaticResource ListBoxItemBorder}" >
      <Grid x:Name="LayoutRoot" Background="White" ShowGridLines="{Binding Path=ShowGridLines}">
      <Grid.RowDefinitions>
        <RowDefinition />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition />
      </Grid.ColumnDefinitions>
      <CheckBox x:Name="IsChecked"
                Grid.Row="0"
                Grid.Column="0"
                VerticalAlignment="Center"
                />
      
      <ContentControl Grid.Row="0"
                      Grid.Column="1"
                      x:Name="PhrasesViewModel" 
                      HorizontalContentAlignment="Stretch"
                      />

    </Grid>
    </Border>
  </Border>
</UserControl>
